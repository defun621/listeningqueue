# M1 Acceptance Record

Status: Accepted
Candidate: `f6508a9be8856bc5d87ac74bd6725ac17f11aa1e`
Accepted on: 2026-08-27

## Automated evidence

- Persistence scenarios cover migrations, stable Item identity, seen history,
  explicit `Next` placement and ordering, playback state, tombstones, rollback,
  configured data directories, concurrent placement, and portable Item payloads
- The one-command acceptance harness initially failed because the earlier tool
  required several manually coordinated commands
- The screenshot-evidence harness initially failed because the tool accepted no
  evidence directory and produced no PNG artifacts
- `raco test -x .`: 40 tests passed after implementation
- `raco make` for the acceptance tool and its harness: passed
- The final candidate's acceptance script completed all persistence checks,
  cleaned its temporary SQLite database, and produced three deterministic
  1280x720 PNG evidence cards plus an HTML review page
- All three final-candidate screenshots were opened during candidate validation;
  they showed the candidate hash, relevant requirements, observed state, and a
  PASS result
- Candidate and `origin/main` were verified at the same full commit hash

## Human result

The user reported:

```text
Gate: M1
Candidate: f6508a9
Evidence: <截图目录>
Screenshots-reviewed: 3/3
Result: PASS
```

The user reported that all three screenshots were reviewed. The evidence field
contained the template placeholder rather than a concrete local path, so this
record does not invent or retain an evidence-directory location. M1 is accepted.
Production implementation for M2 may begin after this record is committed and
pushed.
